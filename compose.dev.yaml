services:
  agent-server:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=dev
    env_file:
      - .env.dev
    networks:
      - interview-network

networks:
  interview-network:
    external: true

